import { Component } from '@angular/core';

@Component({
    selector: 'app-container',
    templateUrl: './container.component.html', // ./ because it's in the current directory
    styleUrls: ['./container.component.css'] // array with all the style sheets
})
export class ContainerComponent{

}